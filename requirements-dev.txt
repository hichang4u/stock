-r requirements.txt

# pytest / pytest-asyncio는 requirements.txt가 고정한다. 여기서 다시 고정하면
# -r가 가져온 핀과 충돌해 pip가 ResolutionImpossible로 죽는다 — 최초 커밋부터
# 그랬고, 그래서 아래 개발 도구들이 한 번도 설치되지 않았다.

# HTTP mock (KIS API 테스트용)
respx==0.21.1

# mock 유틸
pytest-mock==3.14.0

# 코드 스타일 / 린트
ruff==0.4.10

# 타입 검사
mypy==1.10.1
